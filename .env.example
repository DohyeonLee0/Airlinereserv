DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=airline_reservation
AUTH_SECRET=change-me-in-production

# Optional — enables LLM answers via RAG (without it, retrieved doc excerpts are shown)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
